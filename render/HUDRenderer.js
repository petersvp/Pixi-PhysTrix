/**
 * Owns the Pixi container reserved for score and status displays.
 *
 * This module is part of the render layer of PhysTrix.
 * Its exports are consumed by the modular application runtime.
 */

export class HUDRenderer {
  constructor(root) {
    this.root = root;
  }
}
